# A mock HMI Interface
This app is made with Nextjs using TypeScript, handling CRUD operation and authentication. Tech Stack is Nextjs, Postgresql, prisma, tailwingcss + shadcn/ui, jsonwebtoken, zod for validation.

## Navigate to the root directory and run 'npm run dev' to start this project.